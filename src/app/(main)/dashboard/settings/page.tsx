import { OperationsScreen } from "@/components/operations/operations-screen";

export default function SettingsPage() {
	return (
		<OperationsScreen
			mode="generic"
			title="Settings"
			description="Application settings linked to global company configuration."
		/>
	);
}
