import { ReactNode } from "react";

export function VendorPerformanceModal({ children, vendorName }: { children: ReactNode, vendorName: string }) {
  // Dummy component to fix build since the original was missing from the repository
  return <>{children}</>;
}
